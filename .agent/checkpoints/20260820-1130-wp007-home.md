# Checkpoint — WP-007 Home feed + Shorts firewall

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M4 / G4
- Packet: WP-007-HOME-SHORTS-FIREWALL
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `Sources/FocusTubeCore/Home/HomeFeedAggregator.swift`: aggregates the subscription feed via `YouTubeAPI.fetchSubscriptionFeed` and applies `ShortFormPolicy` (`<=180s`) before render. Pure `filterShortForm` is deterministically testable.
- `Tests/FocusTubeCoreTests/HomeFeedAggregatorTests.swift`: short-form removed before render; unknown-duration kept; pure filter test.
- `App/Home/HomeFeedStore.swift`: `@MainActor @Observable` store with explicit `load()` / `loadMore()` (no infinite pagination), typed `YouTubeAPIError`, auth restore.
- `App/RootView.swift`: Home tab now renders `HomeFeedView` backed by `HomeFeedStore` (GoogleSignInAuthSession + YouTubeDataClient), with explicit "Load more" and a retained M1 player demo entry.

## Acceptance mapping
- subscription uploads aggregate/hydrate deterministically — `YouTubeDataClient.fetchSubscriptionFeed` aggregation test (WP-006) + aggregator.
- <=180s entries are filtered before rendering — `HomeFeedAggregator.filterShortForm` + tests; `ShortFormPolicyTests` 180/181 boundary.
- /shorts/ routes are blocked — `ShortFormPolicy.isBlocked(urlPath:)` + tests.
- explicit load-more replaces infinite scrolling — `HomeFeedStore.loadMore` only on user action; no auto-pagination.

## Known failures or deferred items
- Live Home feed requires authenticated Google credentials (safe dev config); not executed here. The firewall + aggregation are deterministically tested with a stub API.

## Durable decisions made
- None new; Shorts hard-disabled invariants preserved; auth decoupled from extraction.

## Exact next waypoint
- WP-008-SEARCH: explicit-submit search service over `YouTubeAPI` (search.list + hydrate), `ShortFormPolicy` before render, pagination/quota typed UX. Deterministic explicit-submit + filter + quota tests.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests)
```
