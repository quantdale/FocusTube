# Checkpoint — WP-001 Local-only YouTubeKit extraction boundary

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M1 / G1
- Packet: WP-001-EXTRACTION
- State: complete (deterministic gate satisfied)
- Commit: 2f5fe1c

## What changed
- `Sources/FocusTubeCore/Media/ResolvedMedia.swift`: `MediaStream` + `ResolvedMedia` normalized, YouTubeKit-free models.
- `Sources/FocusTubeCore/Media/MediaExtracting.swift`: `MediaExtracting` protocol boundary.
- `Sources/FocusTubeCore/Media/MediaStreamFilter.swift`: hard `{1080,720,480,360}` resolution filter and online-stream selection that never exceeds 1080p.
- `Sources/FocusTubeCore/Media/ExtractionError.swift`: typed `ExtractionError`.
- `App/Media/YouTubeKitMediaExtractor.swift`: concrete `YouTubeKitMediaExtractor` (`.local` method only), maps to normalized models, applies allowed-resolution filter, typed error mapping.
- `App/FocusTubeApp.swift`: re-exports `FocusTubeCore` so test targets see models without re-linking the package.
- `FocusTubeTests/LiveExtractionSmoke.swift`: opt-in live smoke (skipped unless `FOCUSTUBE_LIVE_SMOKE=1`).
- `project.yml`: added `FocusTubeTests` unit-test target (depends only on the app to avoid duplicate-symbol linking of YouTubeKit/FocusTubeCore); included in scheme.

## Acceptance evidence
- `swift test` (FocusTubeCore) — **pass** — run 32342460929 step "Test FocusTubeCore: success"; `MediaExtractionTests` cover disallowed-resolution filtering (1440/2160 excluded), highest-allowed native combined selection, no >1080 selection, and typed error from `FakeMediaExtractor`.
- iOS build/test — **pass** — run 32342460929 step "Build and test: success" (Xcode 26.6, iPhone 17 Pro iOS-26-4); LaunchTests root tabs pass; live smoke skipped by default.
- No feature layer imports YouTubeKit directly: extraction is isolated in `YouTubeKitMediaExtractor` conforming to `MediaExtracting`; UI uses the protocol boundary only.
- No 1440p/2160p escapes normalization: enforced by `MediaStreamFilter.allowed` and covered by `testDisallowedResolutionsAreFiltered`.

## Known failures or deferred items
- Live local extraction (`FOCUSTUBE_LIVE_SMOKE=1`) was not executed in this run (network-independent CI by design); the capability is implemented and the smoke is opt-in. Run it in an environment with network + the env var to observe G1 live evidence.
- Physical-device checks deferred to G10.

## Durable decisions made
- None new; all locked decisions preserved (YouTubeKit local-only, XcodeGen generation, no committed `.xcodeproj`, resolution ceiling 1080p).

## Exact next waypoint
- WP-002-PLAYBACK: prove native online playback via AVPlayer/AVPlayerViewController. Add app-side `PlayerCoordinator` (AVPlayer/AVPlayerViewController) over a selected native-playable combined stream, typed playback failures, and (opt-in/simulator) playback-start smoke. Deterministic selection already lives in `MediaStreamFilter.selectOnlineStream`.

## Resume commands
```text
git pull origin main
swift test                # FocusTubeCore deterministic tests (on macOS CI host)
git push origin main      # triggers ios-ci.yml (build + LaunchTests + skipped live smoke)
FOCUSTUBE_LIVE_SMOKE=1 xcodebuild test ...   # for live extraction evidence
```
