# Checkpoint — WP-006 Auth + YouTube Data API

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M3 / G3
- Packet: WP-006-AUTH-DATA-API
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `Sources/FocusTubeCore/YouTube/VideoSummary.swift`: normalized `VideoSummary` with ISO-8601 `PT#H#M#S` duration parsing so durations are known before short-form eligibility.
- `Sources/FocusTubeCore/YouTube/YouTubeAPI.swift`: `YouTubeAPIError` (unauthorized/quotaExceeded/notFound/network/decode/unknown), `HTTPPerforming` seam, `YouTubeAPI` protocol + `fetchSubscriptionFeed` convenience composing subscriptions -> playlistItems -> videos.
- `Sources/FocusTubeCore/YouTube/YouTubeDataClient.swift`: `YouTubeDataClient` over `HTTPPerforming` with pure `mapError` and request building. No token logging.
- `Tests/FocusTubeCoreTests/YouTubeDataClientTests.swift`: deterministic decode, all error classes, network/decode mapping, and subscription-feed aggregation via scripted performer.
- `App/Auth/AuthSession.swift`: `AuthSession` protocol + `FakeAuthSession`.
- `App/Auth/GoogleSignInAuthSession.swift`: concrete `GIDSignIn` adapter (stable closure-based API + continuations), no token logging.
- `FocusTubeTests/AuthSessionTests.swift`: deterministic fake-auth behavior.

## Acceptance mapping
- fake auth supports deterministic authenticated-app tests — `FakeAuthSession` + `AuthSessionTests`.
- typed YouTube API client handles success/auth/quota/network/decode error classes — `YouTubeDataClient` + `YouTubeDataClientTests`.
- subscription-list integration is implemented and deterministically tested — `fetchSubscriptionFeed` aggregation test.
- no secret/token logging — `YouTubeDataClient` never prints tokens; `GoogleSignInAuthSession` only reads `accessToken?.tokenString`.

## Known failures or deferred items
- Real Google sign-in / subscription smoke requires a configured client ID (safe dev config); not executed here. The boundary is implemented and the fake path is deterministically tested, so unrelated implementation proceeds.

## Durable decisions made
- None new; media extraction remains decoupled from OAuth; 1080p ceiling preserved.

## Exact next waypoint
- WP-007-HOME-SHORTS-FIREWALL: `HomeFeedAggregator` over `YouTubeAPI` applying `ShortFormPolicy` (<=180s and `/shorts/` rejected) before render, with explicit load-more. Deterministic aggregator + ShortFormPolicy regression tests.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests)
```
