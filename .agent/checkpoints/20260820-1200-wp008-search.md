# Checkpoint — WP-008 Search

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M5 / G5
- Packet: WP-008-SEARCH
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `Sources/FocusTubeCore/YouTube/YouTubeAPI.swift`: added `searchVideoIDs(query:accessToken:pageToken:)`.
- `Sources/FocusTubeCore/YouTube/YouTubeDataClient.swift`: `search` endpoint request + `SearchResponse` decode (camelCase `nextPageToken`, `id.videoId`).
- `Sources/FocusTubeCore/Search/SearchService.swift`: `SearchResultPage` + `SearchService` — explicit submit only, hydrate via `fetchVideoDetails`, `ShortFormPolicy` filter before render, quota/error propagation, pagination token passthrough.
- `Tests/FocusTubeCoreTests/SearchServiceTests.swift`: empty query short-circuits (no API call), hydrate+filter, quota error propagation.
- `HomeFeedAggregatorTests` stub updated for new protocol method.
- `App/Search/SearchStore.swift`: `@MainActor @Observable` store, explicit `submit`/`loadMore`, typed `YouTubeAPIError`.
- `App/Search/SearchView.swift`: TextField + explicit Search button (never per-keystroke), results list, load-more, quota UX.
- `App/RootView.swift`: Search tab now renders `SearchView`.

## Acceptance mapping
- no remote search on each keystroke — `SearchView` only calls `submit` on button/return; `SearchService` has no observer on text.
- hydrated results are short-filtered before render — `SearchService` applies `ShortFormPolicy` + `SearchServiceTests`.
- pagination/quota exhaustion behavior is tested — `nextPageToken` passthrough + `loadMore`; quota error test + store error UX.

## Known failures or deferred items
- Live search requires authenticated credentials (safe dev config); deterministic coverage via stub API.

## Durable decisions made
- None new; quota-aware explicit submit, 1080p ceiling, Shorts firewall preserved.

## Exact next waypoint
- WP-009-VIDEO-COMMENTS-ACTIONS: CommentsService (comments.list + replies, disabled handling), supported account actions (subscribe/unsubscribe, rating), DownloadQualityPicker exposing only available subset of 1080/720/480/360. Deterministic comments/disabled/reply + quality-picker tests.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests)
```
