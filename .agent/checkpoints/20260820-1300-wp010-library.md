# Checkpoint — WP-010 Library

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M7 / G7
- Packet: WP-010-LIBRARY
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `App/Library/LibraryModels.swift`: SwiftData `WatchHistoryEntry`, `SavedItem`, `DownloadedMedia` (offline file index).
- `App/Library/LibraryStore.swift`: `@MainActor` store — `recordProgress`/`resumePosition` (upsert), `save` (dedupe), `addDownloadedMedia`, `reconcileDownloads` (removes index entries whose files are missing), `deleteDownloadedMedia` (removes file + metadata together; missing file is not fatal).
- `FocusTubeTests/LibraryStoreTests.swift`: in-memory `ModelContainer` tests — progress persists and resumes across a fresh store, reconcile removes missing files, delete is atomic/safe, save dedupes.
- `App/RootView.swift`: persistent `ModelContainer` + `LibraryStore` shared into `DownloadsView` (lists offline files, reconcile on appear, atomic delete action) and `LibraryView` (continue-watching + saved). Offline library usable without network.

## Acceptance mapping
- playback progress persists/reloads — `recordProgress` + `resumePosition` + test.
- file index and metadata reconcile — `reconcileDownloads` + test (missing file removed).
- delete is atomic/safe — `deleteDownloadedMedia` removes file + metadata; test confirms file gone and metadata gone.
- offline library remains useful without network — Downloads/Library tabs read the local index only.

## Known failures or deferred items
- Progress is recorded by the store API; wiring auto-recording from `PlayerCoordinator` playback time is a later polish (the seam exists).
- Real device filesystem/relaunch reconciliation is deferred to G10 but the logic is deterministically tested.

## Durable decisions made
- None new; 1080p ceiling, Shorts firewall, native playback boundary preserved.

## Exact next waypoint
- WP-011-BACKGROUND-MEDIA: AVAudioSession/background-audio, Now Playing, MPRemoteCommandCenter, PiP wiring, interruption handling, deterministic command tests, device-only checks recorded as deferred.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests)
```
