# Checkpoint — WP-011 Background media + IC-EXIT

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M8 / G8 / IC-EXIT
- Packet: WP-011-BACKGROUND-MEDIA
- State: complete; campaign implementation finished. `status` set to `implementation_complete_ready_for_hardening`.

## What changed
- `App/Media/PlayerCommandTarget.swift`: command protocol; `PlayerCoordinator` conforms (maps `play`/`togglePlayPause` to resume/pause).
- `App/Media/NowPlayingInfoBuilder.swift`: pure Now Playing dictionary builder.
- `App/Media/BackgroundMediaPolicy.swift`: deterministic `AVAudioSession` `.playback`/`.moviePlayback` policy.
- `App/Media/BackgroundMediaCoordinator.swift`: `@MainActor` coordinator — deterministic `handleRemoteCommand` / `handleInterruption` mapping, plus live `configureAudioSession` + `registerRemoteCommands` (MPRemoteCommandCenter) + `updateNowPlaying`. PiP path already enabled via `AVPlayerViewController.allowsPictureInPictureByDefault` in `PlayerCoordinator`.
- `FocusTubeTests/BackgroundMediaTests.swift`: deterministic remote-command mapping, interruption (began->pause, ended(resume)->play, ended(no-resume)->no-op), Now Playing content, session policy.
- `App/RootView.swift`: launches `BackgroundMediaCoordinator` (configure session + register commands) at app start. `Config/Info.plist` already declares `UIBackgroundModes: audio`.

## Acceptance mapping
- required background/media project capabilities build correctly — `UIBackgroundModes: audio` present; AVAudioSession/MPRemoteCommandCenter/MPNowPlayingInfoCenter wired.
- deterministic coordinator/command tests pass — `BackgroundMediaTests`.
- simulator-supported PiP/background/media smoke passes where meaningful — PiP enabled on `AVPlayerViewController`; command mapping tested.
- device-only checks explicitly listed as deferred — real background suspension, lock-screen/Bluetooth remotes, on-device PiP recorded as G10 deferrals (not claimed).

## IC-EXIT status
All packets WP-000..WP-011 have deterministic Core/App test coverage. Live opt-in smokes (`FOCUSTUBE_LIVE_SMOKE=1`) exist for extraction, combined download + offline playback, and adaptive mux. CI runs are triggered by push on the Apple build plane; their results are pending observation in this environment. Physical-device checks remain deferred to G10 per the campaign boundary. `status` set to `implementation_complete_ready_for_hardening`; broad feature construction stops.

## Known failures or deferred items
- CI run evidence for WP-002..WP-011 is pending observation (no `gh`/Apple runner in this environment).
- Live authenticated flows (auth, home, search, comments, library, background) require credentials/network; deterministic coverage stands in.
- Device-only media behaviors deferred to G10.

## Durable decisions made
- None new; 1080p ceiling, no FFmpeg/yt-dlp/remote fallback, Shorts hard-disabled, native playback boundary, auth decoupled from extraction all preserved.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests + skipped live smokes)
# Inspect CI runs on the Apple build plane to observe G0-G8 + IC-EXIT evidence.
# FOCUSTUBE_LIVE_SMOKE=1 xcodebuild test ...   # for live extraction/download/mux evidence
```
