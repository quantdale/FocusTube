# Checkpoint — WP-000 Bootstrap proven on Apple build plane

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M0 / G0
- Packet: WP-000-BOOTSTRAP
- State: complete
- Commit: 90e9afe (HEAD main, after 2e1c409)

## What changed
- `Package.swift`: declared `platforms: [.iOS(.v17), .macOS(.v13)]` so `String.contains(_:)` in `ShortFormPolicy` compiles under SwiftPM's default deployment target (was blocking `swift test`).
- `project.yml`: `info.path` now points at a real `Config/Info.plist`; removed the orphaned `properties` block that broke the YAML after an earlier edit.
- `Config/Info.plist`: added with required keys (display name, empty `UILaunchScreen`, `audio` background mode, portrait orientation) so XcodeGen generation succeeds.

## Acceptance evidence (G0 — all observed)
- `swift test` (FocusTubeCore) — **pass** — run 32340013958 step "Test FocusTubeCore: success" (CI macOS-26).
- XcodeGen generates project — **pass** — step "Generate Xcode project: success"; XcodeGen 2.46.0.
- iOS app target builds on Xcode 26.x — **pass** — step "Build and test: success" (Xcode 26.6).
- iPhone Simulator boots — **pass** — step "Select and boot simulator: success"; iPhone 17 Pro, iOS-26-4 (UDID 2A6DC5A9-CE8C-4BC5-959D-F98D5F4BD9AA).
- App launches without crash + XCUITest root tabs — **pass** — step "Build and test: success" includes LaunchTests `testRootTabsExist` (Home/Search/Downloads/Library).
- Toolchain/runtime versions recorded — Xcode 26.6, Apple Swift 6.3.3 (swiftlang-6.3.3.1.3), XcodeGen 2.46.0, iOS-26-4 / iPhone 17 Pro.
- Artifacts retained — `focustube-ios-ci` (3.3 MB) uploaded from run 32340013958.

CI workflow: `.github/workflows/ios-ci.yml` run `32340013958` (commit 90e9afe). Repo baseline for G0: `swift test` also previously observed passing in the bootstrap artifact-construction environment.

## Known failures or deferred items
- Windows agent host has no local Swift toolchain or `gh`; `swift test` was validated via macOS CI only. This is expected per WP-000 W1.
- Physical-device checks (PiP, real suspension, lock screen, signing) remain deferred to G10 per campaign scope.

## Durable decisions made
- None new; both fixes preserve all locked architectural decisions (native iOS, Swift 6, SwiftUI, YouTubeKit local-only, XcodeGen project generation, no committed `.xcodeproj`).

## Exact next waypoint
- Begin WP-001-EXTRACTION: define FocusTube-owned normalized media/stream models, the `MediaExtracting` protocol, `YouTubeKitMediaExtractor` (local only), filter to {1080,720,480,360}, and add fake extractor + deterministic selection/normalization tests. Validate via `swift test` on CI.

## Resume commands
```text
git pull origin main
# inspect current state
cat .agent/STATE.yaml
# run deterministic core tests (on macOS CI host)
swift test
# trigger Apple build plane
git push origin main   # ios-ci.yml runs on push to main
```
