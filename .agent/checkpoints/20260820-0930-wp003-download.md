# Checkpoint — WP-003 Combined download + offline playback

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M1 / G1
- Packet: WP-003-COMBINED-DOWNLOAD
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `Sources/FocusTubeCore/Download/DownloadState.swift`: explicit `DownloadStatus` + `DownloadState` machine (idle/queued/downloading/paused/finalizing/completed/failed) with validated transitions and typed `DownloadError`.
- `Sources/FocusTubeCore/Download/DownloadTask.swift`: `DownloadTask` + `DownloadRequest` models.
- `Sources/FocusTubeCore/Download/DownloadTransport.swift`: `DownloadTransport` seam, `DownloadEvent`, and `FileManaging` seam (with `FileManager` conformance) for deterministic filesystem behavior.
- `Sources/FocusTubeCore/Download/DownloadCoordinator.swift`: `actor` owning the state machine, progress, atomic finalization via `FileManager.replaceItemAt`, and validation (exists + size > 0). Network/FS injected; deterministic without real transfers.
- `App/Download/URLSessionDownloadTransport.swift`: concrete `URLSession` download-task transport proving the allowed combined stream downloads through the URLSession path.
- `App/Playback/PlayerCoordinator.swift`: added `playLocalFile(_:)` for offline native playback from a finalized local file.
- `FocusTubeTests/DownloadCoordinatorTests.swift`: deterministic state-machine + finalization/validation tests via `NoopDownloadTransport` + `FakeFileManager`.
- `FocusTubeTests/OfflinePlaybackTests.swift`: deterministic proof a local file creates an `AVPlayerItem` with no network.
- `FocusTubeTests/DownloadLiveSmoke.swift`: opt-in (`FOCUSTUBE_LIVE_SMOKE=1`) combined download + offline playback smoke.

## Acceptance mapping
- allowed combined stream downloads through URLSession path — `URLSessionDownloadTransport` + `DownloadLiveSmoke`.
- file is atomically finalized and validated — `DownloadCoordinator.finalize` (replaceItemAt + validation) + `DownloadCoordinatorTests`.
- local file plays through native player without network dependency — `PlayerCoordinator.playLocalFile` + `OfflinePlaybackTests`.

## Known failures or deferred items
- Deterministic tests and implementation are complete; live URLSession download/offline playback was not executed (no `FOCUSTUBE_LIVE_SMOKE=1` + network in isolated run). The capability is implemented; run the opt-in smoke on a network-enabled Apple CI host for G1 live evidence.
- Durable background transfer (resume across suspension/relaunch, task reconciliation) is WP-005, not this packet.

## Durable decisions made
- None new; resolution ceiling 1080p, no FFmpeg/yt-dlp/remote fallback preserved.

## Exact next waypoint
- WP-004-ADAPTIVE-1080: prove 1080p video-only + audio download and native mux path. Add component selection, per-component download, and native AVFoundation export/mux (or document incompatibility via ADR review). Add deterministic selection + mux-policy tests.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests + skipped live smoke)
FOCUSTUBE_LIVE_SMOKE=1 xcodebuild test ...   # for live combined-download + offline playback evidence
```
