# Checkpoint — WP-009 Video page, comments, actions, quality picker

- Campaign: IMPLEMENTATION_V1
- Milestone/Gate: M6 / G6
- Packet: WP-009-VIDEO-COMMENTS-ACTIONS
- State: complete (implementation + deterministic tests; CI evidence pending observation)
- Commit: pending-ci (push triggers ios-ci)

## What changed
- `Sources/FocusTubeCore/Media/Comment.swift`: `Comment` + `CommentPage` (with `disabled` state).
- `Sources/FocusTubeCore/YouTube/VideoRating.swift`: `VideoRating` enum.
- `Sources/FocusTubeCore/YouTube/YouTubeAPI.swift`: added `fetchComments`, `subscribe`, `unsubscribe`, `rateVideo`; added `.commentsDisabled` error.
- `Sources/FocusTubeCore/YouTube/YouTubeDataClient.swift`: `commentThreads` endpoint + decode, `subscribe`/`unsubscribe`/`rateVideo` mutations, and `commentsDisabled` detection (403 reason) in `apiError`.
- `Sources/FocusTubeCore/Media/CommentsService.swift`, `Sources/FocusTubeCore/YouTube/AccountActionsService.swift`, `Sources/FocusTubeCore/Download/DownloadQualityPicker.swift` (never fabricates a quality).
- `Tests/FocusTubeCoreTests/VideoActionsTests.swift` (disabled state, threads+replies, disabled error, subscribe/rate, quota error) and `DownloadQualityPickerTests.swift`.
- `App/Video/VideoPageView.swift` + `DownloadQualityPickerView.swift`: native player + available-only quality picker + comments (disabled handled) + actions. Home/Search selection now opens `VideoPageView` via shared `AuthSession` + `YouTubeAPI`.

## Acceptance mapping
- comments read/pagination works — `CommentsService` + `VideoActionsTests` (threads+replies, nextPageToken).
- comments-disabled state works — `CommentPage.disabled` + `.commentsDisabled` error propagation + UI handling.
- subscribe/unsubscribe and selected rating action are covered — `AccountActionsService` + tests (subscribe/rate recorded; unsubscribe callable).
- download picker contains only the actually available subset of 1080/720/480/360 — `DownloadQualityPicker` + tests; UI uses only resolved-media qualities.

## Known failures or deferred items
- Live comments/actions require authenticated credentials (safe dev config); deterministic coverage via full stub API. Real mutation smoke is optional per G3/G6.

## Durable decisions made
- None new; Shorts firewall, 1080p ceiling, native playback boundary preserved.

## Exact next waypoint
- WP-010-LIBRARY: SwiftData watch-history/resume/saves + LibraryStore (persists progress, reconciles offline-media index with filesystem, atomic delete), deterministic reconciliation/delete tests.

## Resume commands
```text
git pull origin main
# push triggers ios-ci.yml (build + LaunchTests + FocusTubeTests)
```
