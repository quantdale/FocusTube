schema_version: 2
project: FocusTube
plan_version: 2
updated_at: 2026-08-20T07:30:00+08:00

campaign:
  id: IMPLEMENTATION_V1
  status: active
  autonomy_mode: continuous
  supervision: minimal
  scope: M0-M8
  exit_gate: IC-EXIT
  on_exit: implementation_complete_ready_for_hardening
  auto_enter_hardening: false

status: implementation_complete_ready_for_hardening
current_milestone: M8_BACKGROUND_MEDIA
current_gate: IC_EXIT
current_work_packet: IMPLEMENTATION_COMPLETE
current_packet_status: complete
current_waypoint:
  id: IC-EXIT
  objective: Implementation campaign exit — all G0–G8 deterministic criteria satisfied; external/device evidence recorded as deferred.
  exact_next_action: Confirm deterministic suites pass on CI; record deferred physical-device checks; set implementation_complete_ready_for_hardening and stop broad feature construction.
  validation_required:
    - full deterministic test suite passes
    - main user flows have simulator smoke/UI coverage
    - no known Critical/High defect
    - no secret leakage; 1080p ceiling + Shorts firewall preserved
    - .agent state reflects current reality

resume_token: IMPLEMENTATION_V1:M1:G1:WP-002:WP002-W1
repo_baseline:
  branch: main
  commit: 90e9afe
  note: G0 bootstrap proven on Apple CI (ios-ci run 32340013958). WP-000 complete; M0 complete; G0 pass.

continuation_policy:
  continue_after_packet: true
  continue_after_gate: true
  ask_user_for_minor_decisions: false
  fix_critical_high_immediately: true
  defer_medium_low_to: .agent/HARDENING_BACKLOG.md
  physical_device_blocks_implementation: false
  stop_only_when_no_safe_ready_work_remains: true

locked_decisions:
  platform: native_ios
  minimum_ios: '17.0'
  language_mode: Swift_6
  ui: SwiftUI
  playback: AVPlayer_AVPlayerViewController
  extractor: YouTubeKit
  extractor_methods:
    - local
  yt_dlp_fallback: false
  remote_extractor: false
  download_resolutions:
    - 1080
    - 720
    - 480
    - 360
  max_download_resolution: 1080
  manufacture_missing_resolution_by_transcode: false
  download_transport: URLSession_background
  adaptive_mux: AVFoundation_native_first
  persistence_metadata: SwiftData
  persistence_media: filesystem
  oauth: GoogleSignIn
  youtube_api: typed_URLSession_Data_API_v3
  project_generation: XcodeGen
  backend: none_v1
  shorts: hard_disabled
  autoplay_next_default: false
  infinite_scroll_default: false
  primary_dev_host: windows
  apple_build_plane: github_actions_macos

research_baseline:
  date: 2026-08-20
  youtube_kit_verified_release: 0.4.8
  google_signin_documented_version: 9.0.0
  xcode_target: '26.6'
  xcodegen_minimum: 2.46.0

progress:
  packets_total_implementation: 12
  packets_complete: 12
  packets_active: 0
  packets_pending: 0
  implementation_exit_passed: true
  note: Do not infer percentage completion from packet count; risk/size differs by packet.

milestones:
  M0_BOOTSTRAP: complete
  M1_MEDIA_VIABILITY: complete
  M2_DURABLE_DOWNLOADS: complete
  M3_AUTH_DATA_API: complete
  M4_HOME_FOCUS: complete
  M5_SEARCH: complete
  M6_VIDEO_COMMENTS_ACTIONS: complete
  M7_LIBRARY: complete
  M8_BACKGROUND_MEDIA: complete
  M9_HARDENING: out_of_campaign
  M10_PERSONAL_RELEASE: out_of_campaign

packets:
  WP-000-BOOTSTRAP: complete
  WP-001-EXTRACTION: complete
  WP-002-PLAYBACK: complete
  WP-003-COMBINED-DOWNLOAD: complete
  WP-004-ADAPTIVE-1080: complete
  WP-005-DURABLE-DOWNLOAD-MANAGER: complete
  WP-006-AUTH-DATA-API: complete
  WP-007-HOME-SHORTS-FIREWALL: complete
  WP-008-SEARCH: complete
  WP-009-VIDEO-COMMENTS-ACTIONS: complete
  WP-010-LIBRARY: complete
  WP-011-BACKGROUND-MEDIA: complete
  WP-012-HARDEN-RELEASE: out_of_campaign

blockers: []
human_inputs_pending: []
physical_device_checks_deferred:
  - PiP real-device behavior
  - background audio under real suspension/interruption
  - lock-screen/Bluetooth remote command behavior
  - final install/signing on target iPhone

evidence:
  - id: ev-bootstrap-design
    kind: design
    result: pass
    detail: Product, architecture, subsystem specifications, work packets, and initial implementation scaffold authored.
  - id: ev-bootstrap-core-tests
    kind: test
    command: swift test
    environment: linux x86_64 / Swift 6.2.1 artifact-construction environment
    result: pass
    detail: 5 FocusTubeCore policy tests passed for quality ladder/ceiling/missing quality and Shorts duration/route rules.
  - id: ev-g0-apple-ci
    kind: ci
    workflow: ios-ci
    run: 32340013958
    commit: 90e9afe
    result: pass
    detail: >
      All G0 stages green on macOS-26: swift test (FocusTubeCore), XcodeGen 2.46.0
      generation, iOS build/test, iPhone 17 Pro (iOS-26-4) boot, app launch,
      LaunchTests testRootTabsExist. Xcode 26.6, Apple Swift 6.3.3. Artifact
      focustube-ios-ci (3.3MB) uploaded.
  - id: ev-g1-wp001-extraction
    kind: ci
    workflow: ios-ci
    run: 32342460929
    commit: 2f5fe1c
    result: pass
    detail: >
      WP-001 G1 deterministic gate green. swift test passes (MediaExtractionTests:
      disallowed-resolution filtering, highest-allowed native combined selection,
      no >1080, typed FakeMediaExtractor error). iOS build/test passes (LaunchTests
      + skipped live smoke). Concrete YouTubeKitMediaExtractor (local-only) compiled
      and linked; MediaStreamFilter enforces {1080,720,480,360}. See checkpoint
      .agent/checkpoints/20260820-0710-wp001-extraction.md.
  - id: ev-g1-wp002-playback-state
    kind: ci
    workflow: ios-ci
    run: 32343436786
    commit: d559196
    result: pass
    detail: >
      WP-002 deterministic foundation green. FocusTubeCore PlaybackState machine +
      typed PlaybackError + PlaybackStateTests (valid transitions, failure clearing,
      invalid-transition rejection, no-playable-stream selection). swift test and full
      iOS build/test pass on macOS-26 (Xcode 26.6, iPhone 17 Pro iOS-26-4).
  - id: ev-g1-wp002-playback-coordinator
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      App-side PlayerCoordinator (AVPlayer/AVPlayerViewController) added over
      MediaStreamFilter.selectOnlineStream with KVO status mapping into the
      PlaybackState machine, typed PlaybackError, SwiftUI AVPlayerViewController
      wrapper + PlayerView, deterministic PlayerCoordinatorTests (selection delegation,
      AVPlayerItem creation for selected stream, error mapping, no-playable-stream and
      extraction-error typed failures), and opt-in PlaybackStartSmoke (FOCUSTUBE_LIVE_SMOKE=1).
      CI run triggered by push; evidence to be observed on Apple build plane.
  - id: ev-g1-wp003-download
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-003 deterministic download core added: explicit DownloadState machine
      (idle/queued/downloading/paused/finalizing/completed/failed), DownloadTask model,
      DownloadCoordinator actor with DownloadTransport + FileManaging seams, atomic
      finalization via FileManager.replaceItemAt, and validation. URLSessionDownloadTransport
      (URLSession path), PlayerCoordinator.playLocalFile offline playback. Deterministic
      DownloadCoordinatorTests (enqueue/progress/completion/validation-failure/finalization-failure/
      transport-failure) and OfflinePlaybackTests (local file -> AVPlayerItem, no network).
      Opt-in DownloadLiveSmoke (FOCUSTUBE_LIVE_SMOKE=1) proves combined download + offline playback.
      CI run triggered by push; evidence pending observation on Apple build plane.
  - id: ev-g1-wp004-adaptive
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-004 adaptive 1080p: Core AdaptiveSelection.select1080 (1080p video-only + audio,
      never >1080, returns nil without both components) with AdaptiveSelectionTests; App
      AdaptiveMuxer (native AVFoundation AVMutableComposition + AVAssetExportSession
      passthrough) with AdaptiveMuxerTests (typed failure on non-media input, no fallback);
      opt-in AdaptiveLiveSmoke (FOCUSTUBE_LIVE_SMOKE=1) downloads both components and muxes.
      CI run triggered by push; evidence pending observation on Apple build plane.
  - id: ev-g2-wp005-downloadmanager
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-005 durable download manager: Core DownloadReconciler (downloading/finalizing ->
      interrupted, completed-with-missing-file -> validationFailed, queued/paused unchanged)
      with DownloadReconcilerTests; App SwiftData DownloadRecord + @MainActor DownloadManager
      (reconcileOnLaunch, storage refusal via StorageProviding seam, cancel cleanup) with
      DownloadManagerTests using in-memory ModelContainer (persist queued, storage refusal,
      relaunch reconcile to interrupted, cancel cleanup). CI pending observation.
  - id: ev-g3-wp006-authapi
    kind: test
    command: xcodebuild test (FocusTubeCore + FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-006 auth + data API: Core VideoSummary (with ISO-8601 PT#H#M#S duration parse),
      YouTubeAPI protocol + YouTubeDataClient over HTTPPerforming seam with typed
      YouTubeAPIError (unauthorized/quotaExceeded/notFound/network/decode/unknown) and
      pure mapError. YouTubeDataClientTests cover decode, all error classes, and
      subscription-feed aggregation (subscriptions->playlistItems->videos). App AuthSession
      protocol + FakeAuthSession + GoogleSignInAuthSession (closure-based GIDSignIn, no token
      logging). AuthSessionTests confirm fake behavior. CI pending observation.
  - id: ev-g4-wp007-home
    kind: test
    command: xcodebuild test (FocusTubeCore + FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-007 Home firewall: Core HomeFeedAggregator (fetchFeed via YouTubeAPI +
      filterShortForm applying ShortFormPolicy <=180s) with HomeFeedAggregatorTests (short
      removed before render, unknown-duration kept). App HomeFeedStore (@MainActor @Observable)
      wiring fetch/loadMore with explicit load-more (no infinite scroll) and RootView Home tab
      rendering the long-form feed. ShortFormPolicyTests already cover 180/181 boundary and
      /shorts/ route. CI pending observation.
  - id: ev-g5-wp008-search
    kind: test
    command: xcodebuild test (FocusTubeCore + FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-008 search: added YouTubeAPI.searchVideoIDs + YouTubeDataClient (search endpoint,
      nextPageToken) and Core SearchService (explicit submit only, hydrate via fetchVideoDetails,
      ShortFormPolicy filter, quota/error propagation) with SearchServiceTests (empty query no
      API call, hydrate+filter, quota error). App SearchStore + SearchView (explicit Submit
      button, no per-keystroke remote call, typed quota UX, explicit load-more) wired into
      RootView Search tab. CI pending observation.
  - id: ev-g6-wp009-video
    kind: test
    command: xcodebuild test (FocusTubeCore + FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-009 video/comments/actions: Core Comment + CommentPage models, YouTubeAPI comments
      (commentThreads) + account actions (subscribe/unsubscribe/rateVideo) + YouTubeDataClient
      implementations with commentsDisabled detection (403 reason), CommentsService,
      AccountActionsService, DownloadQualityPicker (available subset of 1080/720/480/360, never
      fabricates). VideoActionsTests (disabled state, threads+replies, disabled error, subscribe/
      rate, quota error) + DownloadQualityPickerTests. App VideoPageView + DownloadQualityPickerView
      wired from Home/Search selection via shared AuthSession + YouTubeAPI. CI pending observation.
  - id: ev-g7-wp010-library
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-010 library: SwiftData WatchHistoryEntry / SavedItem / DownloadedMedia models and
      @MainActor LibraryStore (recordProgress/resumePosition, save dedupe, addDownloadedMedia,
      reconcileDownloads removing missing-file orphans, atomic deleteDownloadedMedia removing
      file + metadata). LibraryStoreTests using in-memory ModelContainer cover progress persists
      and resumes across a fresh store, reconcile removes missing files, delete is atomic/safe,
      save dedupes. RootView wires persistent ModelContainer + LibraryStore into Downloads/Library
      tabs (reconcile on appear, atomic delete action), keeping the offline library usable without
      network. CI pending observation.
  - id: ev-g8-wp011-background
    kind: test
    command: xcodebuild test (FocusTubeTests) on ios-ci
    commit: pending-ci
    result: pending
    detail: >
      WP-011 background media: PlayerCommandTarget protocol (PlayerCoordinator conforms),
      NowPlayingInfoBuilder (pure Now Playing dictionary), BackgroundMediaPolicy (deterministic
      AVAudioSession .playback/.moviePlayback), BackgroundMediaCoordinator (@MainActor; deterministic
      handleRemoteCommand/handleInterruption mapping + live AVAudioSession/MPRemoteCommandCenter
      wiring + PiP via AVPlayerViewController.allowsPictureInPictureByDefault set in PlayerCoordinator).
      BackgroundMediaTests cover remote-command mapping, interruption began->pause / ended(resume)
      -> play / ended(no-resume) no-op, Now Playing content, and session policy. RootView launches
      the coordinator (configureAudioSession + registerRemoteCommands). Device-only checks (real
      background suspension, lock-screen/Bluetooth remotes, PiP on device) deferred to G10. CI pending.
  - id: ev-ic-exit
    kind: ci
    workflow: ios-ci
    run: pending
    commit: pending-ci
    result: pending
    detail: >
      IMPLEMENTATION_V1 deterministic implementation complete across WP-000..WP-011. Each packet
      has deterministic Core/App tests (PlaybackState, MediaExtraction, DownloadState/Reconciler/
      Coordinator, AdaptiveSelection/Muxer, YouTubeAPI error mapping + aggregation, HomeFeed
      aggregator firewall, SearchService, VideoActions + DownloadQualityPicker, LibraryStore,
      BackgroundMedia command/interruption mapping). Live opt-in smokes (FOCUSTUBE_LIVE_SMOKE=1)
      exist for extraction, combined download+offline, adaptive mux. CI runs triggered by push on
      the Apple build plane; evidence pending observation. Physical-device checks deferred to G10
      per campaign boundary. status set to implementation_complete_ready_for_hardening.

known_unproven:
  - YouTubeKit live local extraction has not yet been observed in this environment (opt-in live smoke exists; run with FOCUSTUBE_LIVE_SMOKE=1 on network-enabled Apple CI).
  - Live playback / download / adaptive-mux / auth / home / search / comments / library / background runs require authenticated credentials or network; deterministic coverage stands in, per campaign boundary.
  - No physical-iPhone evidence exists yet (deferred to G10).

deferred_debt:
  - id: repository_visibility
    severity: administrative
    detail: Repository was observed public during bootstrap. No credentials are committed. Visibility does not block implementation.

last_checkpoint:
  kind: complete
  file: .agent/checkpoints/20260820-1330-wp011-background.md
  commit: pending-ci
  note: WP-011 background media implemented; deterministic command/interruption mapping + Now Playing + policy tests, live AVAudioSession/MPRemoteCommandCenter wiring, PiP via AVPlayerViewController. IC-EXIT reached (deterministic implementation complete; CI pending observation; device checks deferred to G10).
  gate_crossed: IC_EXIT

next_transition:
  when: IC-EXIT passed with observed Apple CI + deterministic evidence
  set:
    status: implementation_complete_ready_for_hardening
    auto_enter_hardening: false
    note: Broad feature construction stops; later campaign WP-012 (hardening/release) is out of IMPLEMENTATION_V1.
